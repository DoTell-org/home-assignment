import { Settings } from '../assets/icons';

export const WorkspaceSettings = () => {
  return (
    <button className='border-none bg-transparent outline-none flex items-center justify-center cursor-pointer transition-opacity duration-300 ease-in-out p-0 hover:opacity-80'>
      <Settings/>
    </button>
  )
}
